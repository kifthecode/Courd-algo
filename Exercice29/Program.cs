//Un tableau de 6 voyelles initialisé au moment de la déclaration
char[] voyelles = { 'a', 'e', 'i', 'o', 'u', 'y' };

for (int i = 0; i < voyelles.Length; i++)

{   // affichage du contenu du tableau

    Console.WriteLine(voyelles[i]);
}